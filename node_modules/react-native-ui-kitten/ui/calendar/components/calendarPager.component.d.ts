import React from 'react';
import { ViewPagerElement, ViewPagerProps } from '../../viewPager/viewPager.component';
import { CalendarDateInfo } from '../type';
import { Override } from '../../support/typings';
export declare type CalendarPagerProps<D> = Override<ViewPagerProps, {
    data: CalendarDateInfo<D>[];
    children: (date: CalendarDateInfo<D>, index: number) => React.ReactElement<any>;
}>;
export declare type CalendarPagerElement<D> = React.ReactElement<CalendarPagerProps<D>>;
export declare class CalendarPager<D> extends React.Component<CalendarPagerProps<D>> {
    scrollToIndex(params: {
        index: number;
        animated?: boolean;
    }): void;
    private viewPagerRef;
    private onSelect;
    private shouldLoadComponent;
    private createChildElement;
    render(): ViewPagerElement;
}
